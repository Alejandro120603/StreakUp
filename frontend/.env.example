# Local web development proxy only.
NEXT_DEV_API_PROXY_URL=http://localhost:5000

# Explicit runtime mode. Connected MVP builds should keep this `false`.
NEXT_PUBLIC_OFFLINE_MODE=false

# Hosted backend URL for connected web/mobile builds. Do not use localhost in the APK.
NEXT_PUBLIC_API_URL=https://api.example.com
